# BlockEntityType.Builder.of takes a BlockEntitySupplier, which vanilla keeps
# package-private. NeoForge widens it in its own patches, but :common compiles
# against plain NeoForm, so registering a block entity type from shared code
# needs it widened here too.
public net.minecraft.world.level.block.entity.BlockEntityType$BlockEntitySupplier
